function previousCard(state, index) {

  // const newContacts = state.contacts.map((contact, index) => {
  //   let ourContact = Object.assign({}, contact);
  //   if (indexToChange === index) {
  //     ourContact.email = emailToSet;
  //   }
  //
  //   return ourContact;
  // });
  //
  // return Object.assign({}, state, {
  //   contacts: newContacts
  // });
}
